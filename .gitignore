# Raw video / clips — never commit (repo bloat + people's likeness).
# Keep clips locally in spike/clips/ or in Supabase Storage instead.
spike/clips/
*.mp4
*.mov
*.MOV
*.avi
*.mkv
*.webm

# Filled-in labels may reference real people — keep the template, ignore real sets.
spike/eval/labels_*.csv
!spike/eval/labels_template.csv

# Pose model (auto-downloaded, ~6 MB) + test assets + extractor eval output.
spike/models/
*.task
spike/tests/assets/
spike/eval/out/

# Python
__pycache__/
*.pyc
.venv/
venv/

# Env / secrets
.env
.env.*

# OS
.DS_Store
